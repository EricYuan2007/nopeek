// NoPeekCore — pure detection logic, Foundation-only, unit-testable via `swift test`.
//
// The real types (DetectionTypes / DetectionStateMachine) land in milestone M3.
// This placeholder keeps the SwiftPM target non-empty from M0 onward.

public enum NoPeekCore {
    public static let version = "0.1.0"
}
